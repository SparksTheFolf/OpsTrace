{
  "name": "opstrace",
  "version": "0.0.2",
  "description": "OpsTrace to to log EVERYTHING on the site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SparksTheFolf",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "fs": "^0.0.1-security"
  }
}
